# 环境变量配置
# Vercel Postgres 会自动注入 DATABASE_URL，无需手动设置
# 本地开发用 SQLite: DATABASE_URL="file:./dev.db"
DATABASE_URL="postgresql://user:password@host:5432/dbname"
NEXTAUTH_SECRET="请替换为一个随机字符串-至少32位"
NEXTAUTH_URL="http://你的域名或IP:3000"
